import { CMS_NAME } from '../lib/constants'

const Intro = () => {
  return (
    <header className='intro-header'>
      <h1>
        {CMS_NAME}
      </h1>
    </header>
  )
}

export default Intro
